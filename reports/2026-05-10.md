# 2026-05-10 — AIC 챌린지 작업 보고서

## 세션 개요

| 항목 | 값 |
|---|---|
| 시작 상태 | `aic/` 토킷 + `aic_work/` 빈 골조 (`strategy/00_master_plan.md`만 존재) |
| 종료 상태 | 4 commit 완료, GitHub 백업 동기화, pytest 20/20 통과, audit 통과 |
| 커밋 수 | 4 |
| 변경 파일 | 53 (추가 50, 수정 7 — 커밋 간 중복 제외) |
| 변경 lines | +5,766 / −28 |
| 작업 영역 | `/home/nggw/challenge/aic_work/` |

---

## 1. 산출물 요약

### 1.1 GitHub 연결 (commit `83f15fb`)
- `aic_work` → https://github.com/NGGW519/challenge.git (`main`)
- `Initial commit`은 자동 amend로 한국어 한 줄 스타일에 맞춤.
- 비어 있던 6개 placeholder 디렉토리(data/logs/models/policies/scripts/strategy/training)는 git이 빈 디렉토리를 추적하지 않아 첫 commit엔 미포함.

### 1.2 전략 문서 (commit `ea95c96`)
`strategy/`에 11개 markdown (총 ~2,940 lines):
- `00_master_plan.md` — 단독 작업·vast.ai 무제한·마감 변경 반영해 보강
- `01_agent_team.md` — Claude 서브에이전트 페르소나 9종 (인간 팀원 없음 명시)
- `02_vastai_setup.md` — 인스턴스 매핑, R2 영속 스토리지, dev Docker
- `10_phase1_baseline.md` ~ `17_phase8_submission.md` — Phase 1–8 상세 계획

평가 환경 핵심 사실 (출처: `aic/docs/`):
- 평가 인스턴스 = 1× NVIDIA L4 24 GiB / Gazebo / ROS 2 Kilted Kaiju
- 점수 = Tier 1 (0–1) + Tier 2 (-36 ~ +24) + Tier 3 (-12 ~ +75)
- 목표 280+/300 = 75×3 (정확 삽입) + ~18 Tier 2 + 3 Tier 1

### 1.3 인프라 + 자동화 (commit `40174aa`)
- `docker/dev.Dockerfile` — vast.ai 학습/데이터 수집용 (CUDA 12.1 + Pixi + rclone + AWS CLI)
- `docker/submit.Dockerfile` — ECR 제출용 (toolkit base 위에 우리 패키지 + colcon build)
- `docker/baseline-override.yaml` / `eval-override.yaml` — toolkit `docker-compose.yaml` 위에 덧붙여 평가 실행
- `scripts/onstart.sh` — vast.ai 부팅 시 git clone + R2 mount + tmux 자동 셋업
- `scripts/run_baseline.sh` — 토킷 예제 정책 N회 반복 평가
- `scripts/eval_100.sh` — 우리 정책 100 rollout 병렬 평가
- `scripts/train_with_resume.sh` — crash-safe 학습 wrapper
- `scripts/aggregate_scoring.py` — `scoring.yaml` 묶음 → CSV 통계
- `scripts/audit_pre_submit.sh` — 챌린지 규칙 위반 패턴 grep
- `aic_model_pkg/` — ROS 2 ament_python 패키지 (HybridPolicy + safety + geometry + port_detector + act_inference)
- `data/randomize_scene.py` — 무작위 trial 시나리오 생성 (3 시나리오 모두 동작)
- `pyproject.toml`, `.gitignore`, `README.md` 등 메타 파일

### 1.4 학습/데이터/테스트 (commit `421e7fd`)
- `training/train_act.py` — LeRobot ACT fine-tune (resume + ckpt push + smoke 모드)
- `training/yolov8_port.yaml` — port detector 데이터셋 config
- `data/bag_to_lerobot.py` — rosbag2 mcap → LeRobot v2.1 (20 Hz 리샘플)
- `data/auto_label_ports.py` — TF→픽셀 투영 기반 자동 YOLO 라벨링 (skeleton)
- `scripts/visualize_eval.py` — `summary.csv` → HTML 리포트 (히스토그램·박스플롯·tier 분해·baseline 회귀)
- `tests/` — pytest 20개 (geometry 7 / randomize_scene 8 / safety 5)

---

## 2. 결정 사항 (앞으로 영향)

| 결정 | 근거 |
|---|---|
| `aic/`는 read-only, 모든 작업은 `aic_work/`에서 | 토킷 upstream 충돌 회피, 백업 분리 |
| 인간 팀원 없음 → 전략 "team"은 Claude 페르소나 | 사용자 명시 (`feedback_subagent_personas` 메모리) |
| 평가 GPU는 L4 24 GB (공식 동일)로 fix | sim-to-eval GPU mismatch로 인한 점수 회귀 방지 |
| vast.ai 비용 무제한, idle 인스턴스만 정지 | 사용자 정책 (`user_profile` 메모리) |
| 영속 스토리지 3-tier: NVMe(hot) → R2(warm) → HF Hub(cold) | 인스턴스 휘발성 대응 |
| 커밋 메시지: 한국어 한 줄, Claude attribution 없음 | 사용자 요청 (`feedback_commit_style` 메모리) |
| ACT chunk_size=100 (5초 단위), 1초 후 재추론 | strategy/12 §2 |
| Stage A/B/C 분리 (visual / IL / force-guided) | 충돌 −24 회피 우선, 디버깅 용이성 |
| Cartesian impedance만 사용 (joint mode 미사용) | 한 번에 한 모드, 단순화 |

---

## 3. 검증 결과

| 항목 | 결과 |
|---|---|
| `pytest tests/` | **20 passed in 1.30 s** |
| `ruff check` | **0 errors** (pyproject.toml에 ignore 추가 후) |
| `bash scripts/audit_pre_submit.sh` | **PASSED** (false positive 2개 fix 후) |
| `python3 data/randomize_scene.py --scenario {sfp1,sfp2,sc} --seed 1` | 3 시나리오 정상 출력. seed-시나리오 분리 버그 fix 완료 |

핵심 불변식이 자동화로 잡힘:
- 삼각측량 정확도 1 mm 이내 (synthetic test)
- Force watchdog 0.2 초 trigger / threshold 미만에선 trigger 안 됨
- Gripper jitter ±2 mm (challenge spec) 준수
- NIC 슬라이드 [-0.0215, +0.0234] m, yaw ±10°
- 시나리오별 NIC/SC 레일 mutually exclusive

---

## 4. 메모리 저장

`/home/nggw/.claude/projects/-home-nggw-challenge/memory/`에 다음 영속 메모리 추가:
- `project_aic_challenge.md` — 챌린지 전체 컨텍스트
- `user_profile.md` — 한국어, 단독 작업, GitHub `NGGW519`, vast.ai 무제한
- `project_workspace_layout.md` — `aic/` vs `aic_work/` 분리 규칙
- `project_toolkit_constraints.md` — ROS 2 Kilted, lifecycle 60 s, 금지 패턴
- `feedback_subagent_personas.md` — "team" = Claude 페르소나
- `feedback_commit_style.md` — 한국어 한 줄, attribution 없음

---

## 5. 사용자가 준비해야 할 것 (다음 세션 전)

| 우선 | 항목 | 발급처 | 결과 |
|---|---|---|---|
| 1 | vast.ai 계정 + 잔고 | https://vast.ai | account, balance |
| 2 | Cloudflare R2 bucket `aic-ckpts` | https://dash.cloudflare.com → R2 | access key, secret, endpoint |
| 3 | HuggingFace Hub token (write) + private repo | https://huggingface.co/settings/tokens | `HF_TOKEN`, repo (e.g. `nggw519/aic_v0`) |
| 4 | AWS ECR 자격증명 | 챌린지 조직 측 온보딩 이메일 | access key, ECR URI |
| 5 | 제출 포털 계정 | 위 동일 이메일 | 포털 URL, 로그인 정보 |
| 6 | GHCR PAT (선택) | github.com/settings/tokens, `write:packages` | dev image push용 |

`#1 + #2 + #3` 만 있으면 vast.ai 인스턴스에서 학습/데이터 작업 즉시 시작 가능.
`#4`는 Phase 8 (제출) 직전에만 필요.

---

## 6. 다음 세션 후보

| 후보 | 비용 | 외부 의존 | 가치 |
|---|---|---|---|
| (A) HybridPolicy ROS 메시지 발행 코드 채우기 | $0 | 인스턴스 안에서 검증 필요 | 중 |
| (B) GitHub Actions CI (ruff + pytest 자동) | $0 | 없음 | 중 |
| (C) 가짜 `scoring.yaml`로 aggregate/visualize end-to-end | $0 | 없음 | 소 |
| (D) strategy 결정 로그 섹션 채우기 | $0 | 없음 | 소 |
| (E) dev Dockerfile 빌드 + GHCR push | 약간 (이미지 5–8 GB push) | GHCR PAT | 중 |
| (F) **vast.ai 인스턴스 spin-up + Phase 1 baseline 측정** | 1–2 시간 GPU $ | vast.ai 계정 | **대** |

권장 순서: B → C → A → E → F. (B/C는 다음 세션에서 즉시 진행 가능.)

---

## 7. 커밋 로그

| Hash | 메시지 | 변경 |
|---|---|---|
| `83f15fb` | Initial commit | +123 / 1 file |
| `ea95c96` | Phase 1~8 전략 문서 작성 | +2,926 / −15 / 11 files |
| `40174aa` | 인프라/평가 스크립트 + 정책 패키지 골조 추가 | +1,688 / 25 files |
| `421e7fd` | 학습/평가 스크립트, 데이터 변환, 단위 테스트 추가 | +1,029 / −13 / 17 files |

원격: https://github.com/NGGW519/challenge/commits/main

---

## 8. 디렉토리 구조 (세션 종료 시점)

```
aic_work/
├── .gitignore
├── README.md
├── pyproject.toml
├── strategy/                     # 11 markdown — 전략 문서
│   ├── 00_master_plan.md
│   ├── 01_agent_team.md
│   ├── 02_vastai_setup.md
│   └── 10_phase1_baseline.md ~ 17_phase8_submission.md
├── docker/                       # 4 file
│   ├── dev.Dockerfile
│   ├── submit.Dockerfile
│   ├── baseline-override.yaml
│   └── eval-override.yaml
├── scripts/                      # 7 file
│   ├── onstart.sh
│   ├── run_baseline.sh
│   ├── eval_100.sh
│   ├── train_with_resume.sh
│   ├── aggregate_scoring.py
│   ├── audit_pre_submit.sh
│   └── visualize_eval.py
├── data/                         # 3 file
│   ├── randomize_scene.py        # 동작 검증 완료
│   ├── bag_to_lerobot.py         # ROS env에서만 실동작
│   └── auto_label_ports.py       # ROS env에서만 실동작
├── training/                     # 2 file
│   ├── train_act.py
│   └── yolov8_port.yaml
├── tests/                        # 5 file (20 unit test PASS)
│   ├── conftest.py
│   ├── test_geometry.py
│   ├── test_randomize_scene.py
│   └── test_safety.py
├── aic_model_pkg/                # ROS 2 ament_python package
│   ├── package.xml / setup.py / setup.cfg
│   ├── resource/aic_model_pkg
│   └── aic_model_pkg/
│       ├── HybridPolicy.py       # Stage A/B/C 진입점 (TODO 명시)
│       ├── port_detector.py      # YOLOv8 wrapper (가중치 없으면 disabled)
│       ├── act_inference.py      # LeRobot ACT wrapper
│       ├── safety.py             # bbox clamp + force watchdog
│       ├── geometry.py           # 쿼터니언 / 투영 / 삼각측량
│       └── weights/              # .gitkeep만 (가중치는 R2/HF에서 풀)
└── reports/                      # ← 이번에 추가
    ├── README.md
    └── 2026-05-10.md             # 본 문서
```

---

## 9. 통계 한눈

```
Commits:        4
Files added:    50
Files modified: 7
Lines added:    +5,766
Lines deleted:  -28
Tests:          20 PASS / 0 FAIL
Lint (ruff):    0 errors
Audit:          PASSED (non-strict)
Memory entries: 6 (1 user, 2 feedback, 3 project)
```

---

## 10. 변경 로그 (post-commit)

### 2026-05-11 — SOTA 검증 (A8 Researcher) + 설계 보정 fix 3개

**배경**: A8 Researcher 페르소나(general-purpose agent + WebSearch 11회)로 우리 3-stage hybrid 설계를 SOTA 11편(ACT/IndustReal/ResiP/TacDiffusion/Comp-ACT/FARM 등)에 비춰 검증.

**결론**: **조건부 가능** — Stage C(결정론적 admittance + spiral)는 SOTA 호환적 강전략, 단 작은 보정 1–3개 필요.

**즉시 채택 (이번 라운드)**:
- **F1 — Stage B→C 명시적 hysteresis**: ResiP/TacDiffusion 진단(`force buildup → spiral 동안 force penalty 누적`) 대응. `|F| > 5N AND z 거리 ≤ 10mm AND 200ms 유지` 조건. `HybridPolicy._should_transition_to_c()` 추가.
- **F2 — PortDetector conservative threshold**: tier 3 `잘못된 포트 −12` 방어. confidence 0.4→0.85 + epipolar y 일관성 검사 (≤8px). consistent 매칭 실패 시 `None` 반환 → Stage A fallback.
- **F3 — CheatCodeRecorder reference profile 기록**: 권고 #1(Comp-ACT) 데이터 호환성 확보. `task.yaml`에 stage별(approach/alignment/insertion) stiffness · desired_wrench 저장 → 재수집 없이 ACT v2 학습 가능.

**보류 (조건부 발동)**:
- 권고 #2 ResiP residual RL — Stage C 평균 spiral 시간 ≥ 6초이면 Phase 5 이후 발동
- 권고 #3 IsaacLab 추가 학습 — Phase 7 회귀 시 재검토 (평가가 Gazebo이므로 ROI 작음)

**미해결 위험 (코드로 fix됨)**:
- Stage B→C 전환 timing (F1) ✅
- YOLOv8m SC/SFP false positive (F2) ✅
- ACT v1 단순 7-D output → v2 Comp-ACT 전환 시 데이터 호환 (F3) ✅

**보류된 위험 (Phase 1 측정 후 결정)**:
- chunk_size=100 적정성 — Phase 5 평균 점수에서 확인
- CheatCode demo의 jerk 상한 — 실 데이터 확보 후 측정
- Sim-to-eval gap (Gazebo→Gazebo이지만 plugin/solver 버전 차이 가능)

영향 파일:
- `aic_model_pkg/aic_model_pkg/HybridPolicy.py` — F1
- `aic_model_pkg/aic_model_pkg/port_detector.py` — F2
- `aic_model_pkg/aic_model_pkg/CheatCodeRecorder.py` — F3
- 메모리: `feedback_design_decisions.md` 신규 (다음 세션이 권고 채택 결정을 자동 인식)

---

### 2026-05-10 — 백업 전략 단순화: R2 제거, HF Hub 단독

**배경**: 사용자가 자원 준비 부담을 줄이고 싶다고 의사 표시. R2가 추가 자격증명 1개를 더 요구하지만 우리 작업 패턴(작은 ckpt 자주 push, 데이터셋 한 번만 push)에선 속도 차이가 결정적이지 않다고 판단.

**속도 비교 (참고)**:
- 작은 ckpt (~200MB): R2 ~15초 vs HF ~45초 (학습 시간의 ~2%, 무시 가능)
- 데이터셋 (~50GB): R2 ~7분 vs HF ~30분 (한 번만 발생)

**영향받은 파일**:
- `strategy/02_vastai_setup.md` — §2 스토리지 전략을 2-tier로 단순화, §3 onstart 예시·§9 체크리스트 갱신
- `scripts/onstart.sh` — rclone 설정 제거, `huggingface-cli login` + `huggingface-cli download` 사용
- `scripts/train_with_resume.sh` — 최종 ckpt push를 `huggingface-cli upload`로
- `training/train_act.py` — `push_ckpt()`를 `huggingface_hub.HfApi`로 재작성, `--remote` 형식을 `hf://<repo_id>/<path>`로
- `aic_work/README.md` — 사용자 준비 항목에서 R2 제거
- `CLAUDE.md` — 핵심 규칙에 "백업은 HF Hub 단독" 명시
- 메모리: `feedback_backup_storage.md` 신규 추가

**남은 자격증명**: `HF_TOKEN` 1개만 vast.ai env var로 등록하면 됨.
